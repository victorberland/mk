version: '3'
services:
  web:
    image: httpd
    restart: always
    labels:
      - traefik.enable=yes
      - traefik.frontend.rule=Host:mk.victorberland.com
      - traefik.port=80
    volumes:
      - ./dist/:/usr/local/apache2/htdocs/
